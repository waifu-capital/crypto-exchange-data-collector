# AWS credentials for S3 storage (required when storage.mode is "s3" or "both")
AWS_ACCESS_KEY=
AWS_SECRET_KEY=
AWS_REGION=

# Coinbase API credentials (optional - required for orderbook, not needed for trades)
# See config.toml for usage instructions
COINBASE_API_KEY=
COINBASE_API_SECRET_FILE=